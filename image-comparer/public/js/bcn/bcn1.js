var bc1 = (function() {

    var encode = function(imageData) {
        var w = imageData.width;
        var h = imageData.height;
        var src = imageData.data;

        for (var y = 0; y < h; y+=4) {
            for (var x = 0; x < w; x+=4) {

            }
        }
    };

    return {
        defaults: function() {  }
    }

})();
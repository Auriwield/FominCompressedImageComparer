block diff
    #diff
        .full
            .center
                canvas#diff-canvas
        input.diff-bar(type="range", min=0, max="10", value="1", step="0.01")
    script(src='js/tabs/diff.js')